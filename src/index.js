import React from "react";
import ReactDOM from "react-dom";

import Cambiar_c from "./Cambiar_c";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <Cambiar_c/>
  </React.StrictMode>,
  rootElement
);
